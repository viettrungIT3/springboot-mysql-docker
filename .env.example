# ===========================================
# Spring Boot + MySQL + Docker Environment
# ===========================================
# 
# 🔧 QUICK START:
# - Backend API: http://localhost:8080
# - Frontend: http://localhost:3000  
# - Swagger UI: http://localhost:8080/swagger-ui.html
# - MySQL: localhost:3306
# ===========================================

# ------------------ Database Configuration ------------------
# 🗄️ MySQL Database Settings
# 
# MYSQL_ROOT_PASSWORD: Mật khẩu cho root user của MySQL
# MYSQL_DATABASE: Tên database sẽ được tạo tự động
# MYSQL_USER: Username để ứng dụng kết nối database
# MYSQL_PASSWORD: Mật khẩu cho user trên
# MYSQL_PORT: Port MySQL (mặc định 3306)
# MYSQL_HOST: Hostname của MySQL container (không đổi khi dùng Docker)
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=appdb
MYSQL_USER=appuser
MYSQL_PASSWORD=apppass
MYSQL_PORT=3306
MYSQL_HOST=mysql

# 🔗 Database Connection String
# Spring Boot sẽ sử dụng URL này để kết nối MySQL
# Format: jdbc:mysql://host:port/database?options
SPRING_DATASOURCE_URL=jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT}/${MYSQL_DATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
SPRING_DATASOURCE_DRIVER_CLASS_NAME=com.mysql.cj.jdbc.Driver

# ------------------ Backend Configuration ------------------
# 🚀 Spring Boot Application Settings
#
# SPRING_PROFILES_ACTIVE: Môi trường chạy (dev/test/prod)
# BACKEND_PORT: Port cho Spring Boot API
SPRING_PROFILES_ACTIVE=dev
BACKEND_PORT=8080

# 🗃️ JPA/Hibernate Configuration
# 
# JPA_DDL_AUTO: Cách quản lý database schema
#   - none: Không tự động tạo/sửa schema (dùng Flyway migrations)
#   - create: Tạo mới schema mỗi lần khởi động (⚠️ Mất dữ liệu)
#   - update: Cập nhật schema (⚠️ Có thể gây lỗi)
#   - validate: Chỉ kiểm tra schema có đúng không
# JPA_SHOW_SQL: Hiển thị SQL queries trong console (hữu ích cho debug)
JPA_DDL_AUTO=none
JPA_SHOW_SQL=true

# 🌱 Seed Data Configuration (Chỉ cho dev/test)
# 
# APP_SEED_ENABLED: Bật/tắt việc tạo dữ liệu mẫu khi khởi động
# APP_SEED_PRODUCTS: Số lượng sản phẩm mẫu sẽ tạo
# APP_SEED_CUSTOMERS: Số lượng khách hàng mẫu sẽ tạo
APP_SEED_ENABLED=true
APP_SEED_PRODUCTS=20
APP_SEED_CUSTOMERS=10

# 🔐 JWT Authentication Configuration
# 
# JWT_SECRET: Secret key để ký JWT tokens (⚠️ Phải thay đổi trong production!)
# JWT_EXPIRATION: Thời gian hết hạn của token (milliseconds)
#   - 86400000 = 24 giờ
#   - 3600000 = 1 giờ
JWT_SECRET=mySecretKey123456789012345678901234567890
JWT_EXPIRATION=86400000

# 📝 Logging Configuration
# 
# LOG_LEVEL: Mức độ log (DEBUG/INFO/WARN/ERROR)
# DEBUG: Hiển thị tất cả logs (rất chi tiết)
# INFO: Logs thông tin chung (khuyến nghị cho dev)
# WARN: Chỉ warnings và errors
# ERROR: Chỉ errors
LOG_LEVEL=INFO

# ------------------ Frontend Configuration ------------------
# 🌐 Next.js Frontend Settings
# 
# FRONTEND_PORT: Port cho Next.js application
FRONTEND_PORT=3000

# ------------------ CORS Configuration ------------------
# 🌍 Cross-Origin Resource Sharing Settings
# 
# CORS_ALLOWED_ORIGINS: Các domain được phép gọi API từ browser
# Format: domain1,domain2,domain3
# Ví dụ: http://localhost:3000,https://myapp.com
# ⚠️ KHÔNG sử dụng wildcard (*) trong production!
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173

# CORS_ALLOWED_HEADERS: Các headers được phép trong CORS requests
# Thường bao gồm: Authorization, Content-Type, Accept, X-Requested-With, Origin
CORS_ALLOWED_HEADERS=Authorization,Content-Type,Accept,X-Requested-With,Origin

# CORS_ALLOWED_METHODS: Các HTTP methods được phép
# Thường bao gồm: GET, POST, PUT, PATCH, DELETE, OPTIONS
CORS_ALLOWED_METHODS=GET,POST,PUT,PATCH,DELETE,OPTIONS

# CORS_ALLOW_CREDENTIALS: Cho phép gửi credentials (cookies, auth headers)
# true: Cho phép gửi credentials (khuyến nghị cho API)
# false: Không cho phép gửi credentials
CORS_ALLOW_CREDENTIALS=true

# CORS_EXPOSED_HEADERS: Các headers được expose cho client
# Thường bao gồm: Authorization để client có thể đọc token
CORS_EXPOSED_HEADERS=Authorization

# CORS_MAX_AGE: Thời gian cache preflight requests (giây)
# 3600 = 1 giờ (khuyến nghị cho dev)
# 1800 = 30 phút (khuyến nghị cho production)
CORS_MAX_AGE=3600

# CORS_ENABLE_LOGGING: Bật/tắt logging CORS requests
# true: Bật logging (hữu ích cho debug)
# false: Tắt logging (khuyến nghị cho production)
CORS_ENABLE_LOGGING=true

# ------------------ Rate Limiting Configuration ------------------
# 🚦 Rate Limiting Settings using Bucket4j
# 
# RATE_LIMIT_ENABLED: Bật/tắt rate limiting
# true: Bật rate limiting (khuyến nghị cho production)
# false: Tắt rate limiting (chỉ cho development)
RATE_LIMIT_ENABLED=true

# RATE_LIMIT_PUBLIC_REQUESTS: Số requests cho phép cho public endpoints
# Ví dụ: 100 = 100 requests per minute cho /api/v1/products, /actuator/health
RATE_LIMIT_PUBLIC_REQUESTS=100

# RATE_LIMIT_PUBLIC_WINDOW_SECONDS: Thời gian window cho public endpoints (giây)
# Ví dụ: 60 = 1 phút
RATE_LIMIT_PUBLIC_WINDOW_SECONDS=60

# RATE_LIMIT_API_REQUESTS: Số requests cho phép cho API endpoints
# Ví dụ: 200 = 200 requests per minute cho /api/**
RATE_LIMIT_API_REQUESTS=200

# RATE_LIMIT_API_WINDOW_SECONDS: Thời gian window cho API endpoints (giây)
RATE_LIMIT_API_WINDOW_SECONDS=60

# RATE_LIMIT_AUTH_REQUESTS: Số requests cho phép cho auth endpoints (bảo mật cao)
# Ví dụ: 10 = 10 requests per minute cho /auth/** (chống brute force)
RATE_LIMIT_AUTH_REQUESTS=10

# RATE_LIMIT_AUTH_WINDOW_SECONDS: Thời gian window cho auth endpoints (giây)
RATE_LIMIT_AUTH_WINDOW_SECONDS=60

# RATE_LIMIT_ENABLE_LOGGING: Bật/tắt logging rate limit
# true: Bật logging (hữu ích cho debug)
# false: Tắt logging (khuyến nghị cho production)
RATE_LIMIT_ENABLE_LOGGING=true

# RATE_LIMIT_MESSAGE: Thông báo khi vượt rate limit
RATE_LIMIT_MESSAGE=Rate limit exceeded. Please try again later.
